<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.minshuku.mapper.RoomPriceRuleMapper">
  <!-- 料金ルール一覧は客室情報と結合して管理画面へ渡す。 -->
  <resultMap id="PriceRuleResultMap" type="com.example.minshuku.domain.RoomPriceRule">
    <id property="id" column="id" />
    <result property="roomId" column="room_id" />
    <result property="roomNumber" column="room_number" />
    <result property="roomName" column="room_name" />
    <result property="ruleName" column="rule_name" />
    <result property="startDate" column="start_date" />
    <result property="endDate" column="end_date" />
    <result property="pricePerPerson" column="price_per_person" />
    <result property="priority" column="priority" />
    <result property="active" column="active" />
    <result property="note" column="note" />
    <result property="createdAt" column="created_at" />
    <result property="updatedAt" column="updated_at" />
  </resultMap>

  <select id="findAllWithRoom" resultMap="PriceRuleResultMap">
    <!-- 期間別ルールを表示用の客室名称付きで取得する。 -->
    SELECT
      rpr.id,
      rpr.room_id,
      rooms.room_number,
      rooms.room_name,
      rpr.rule_name,
      rpr.start_date,
      rpr.end_date,
      rpr.price_per_person,
      rpr.priority,
      rpr.active,
      rpr.note,
      rpr.created_at,
      rpr.updated_at
    FROM room_price_rules rpr
      JOIN rooms ON rooms.id = rpr.room_id
    WHERE rpr.active = true
    ORDER BY
      rpr.start_date DESC,
      rpr.priority ASC,
      rooms.room_number ASC
  </select>

  <select id="findBestRule" resultMap="PriceRuleResultMap">
    <!-- 対象日付に合致する最優先ルールを 1 件取得する。 -->
    SELECT
      rpr.id,
      rpr.room_id,
      null AS room_number,
      null AS room_name,
      rpr.rule_name,
      rpr.start_date,
      rpr.end_date,
      rpr.price_per_person,
      rpr.priority,
      rpr.active,
      rpr.note,
      rpr.created_at,
      rpr.updated_at
    FROM room_price_rules rpr
    WHERE rpr.room_id = #{roomId}
      AND rpr.active = true
      AND #{stayDate} BETWEEN rpr.start_date AND rpr.end_date
    ORDER BY
      rpr.priority ASC,
      rpr.price_per_person DESC
    LIMIT 1
  </select>

  <insert id="insert" parameterType="com.example.minshuku.domain.RoomPriceRule" useGeneratedKeys="true" keyProperty="id">
    <!-- 料金ルールを登録する。 -->
    INSERT INTO room_price_rules (
      room_id,
      rule_name,
      start_date,
      end_date,
      price_per_person,
      priority,
      active,
      note
    ) VALUES (
      #{roomId},
      #{ruleName},
      #{startDate},
      #{endDate},
      #{pricePerPerson},
      #{priority},
      #{active},
      #{note}
    )
  </insert>

  <select id="countOverlapping" resultType="int">
    <!-- 同一客室の期間重複を検出する。 -->
    SELECT COUNT(*)
    FROM room_price_rules
    WHERE room_id = #{roomId}
      AND active = true
      AND start_date &lt;= #{endDate}
      AND end_date &gt;= #{startDate}
  </select>

  <update id="delete">
    <!-- 単一料金ルールは履歴保持のため無効化する。 -->
    UPDATE room_price_rules
    SET
      active = false,
      updated_at = now()
    WHERE id = #{id}
      AND active = true
  </update>

  <update id="deleteByIds">
    <!-- 一覧の複数選択削除も履歴保持のため無効化する。 -->
    UPDATE room_price_rules
    SET
      active = false,
      updated_at = now()
    WHERE active = true
      AND id IN
    <foreach collection="ids" item="id" open="(" close=")" separator=",">#{id}</foreach>
  </update>
</mapper>
