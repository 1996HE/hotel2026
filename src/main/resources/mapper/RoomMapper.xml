<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.minshuku.mapper.RoomMapper">
  <!-- 客室一覧・予約可否・状態更新で共通利用する列定義。 -->
  <resultMap id="RoomResultMap" type="com.example.minshuku.domain.Room">
    <id property="id" column="id" />
    <result property="roomNumber" column="room_number" />
    <result property="roomName" column="room_name" />
    <result property="roomType" column="room_type" />
    <result property="capacity" column="capacity" />
    <result property="basePricePerPerson" column="base_price_per_person" />
    <result property="privateBath" column="private_bath" />
    <result property="occupancyStatus" column="occupancy_status" />
    <result property="cleaningStatus" column="cleaning_status" />
    <result property="active" column="active" />
    <result property="note" column="note" />
    <result property="createdAt" column="created_at" />
    <result property="updatedAt" column="updated_at" />
  </resultMap>

  <!-- 客室一覧の共通列。論理削除を含むため、active も取得する。 -->
  <sql id="RoomColumns">
    id,
    room_number,
    room_name,
    room_type,
    capacity,
    base_price_per_person,
    private_bath,
    occupancy_status,
    cleaning_status,
    active,
    note,
    created_at,
    updated_at
  </sql>

  <select id="findAll" resultMap="RoomResultMap">
    <!-- 有効客室を管理画面用に並べる。 -->
    SELECT <include refid="RoomColumns" />
    FROM rooms
    WHERE active = true
    ORDER BY room_number
  </select>

  <select id="findInactive" resultMap="RoomResultMap">
    <!-- 論理削除済み客室は再有効化候補として表示する。 -->
    SELECT <include refid="RoomColumns" />
    FROM rooms
    WHERE active = false
    ORDER BY updated_at DESC, room_number
  </select>

  <select id="findActive" resultMap="RoomResultMap">
    SELECT <include refid="RoomColumns" />
    FROM rooms
    WHERE active = true
    ORDER BY room_number
  </select>

  <select id="findBookable" resultMap="RoomResultMap">
    <!-- 予約フォームには空室かつ清掃済みの客室だけを渡す。 -->
    SELECT <include refid="RoomColumns" />
    FROM rooms
    WHERE active = true
      AND occupancy_status = 'vacant'
      AND cleaning_status = 'cleaned'
    ORDER BY room_number
  </select>

  <select id="findById" resultMap="RoomResultMap">
    SELECT <include refid="RoomColumns" />
    FROM rooms
    WHERE id = #{id}
  </select>

  <select id="findByIdForUpdate" resultMap="RoomResultMap">
    <!-- 予約時はロック付きで読み、二重予約を避ける。 -->
    SELECT <include refid="RoomColumns" />
    FROM rooms
    WHERE id = #{id}
    FOR UPDATE
  </select>

  <select id="findByRoomNumberIncludingInactive" resultMap="RoomResultMap">
    SELECT <include refid="RoomColumns" />
    FROM rooms
    WHERE room_number = #{roomNumber}
  </select>

  <insert id="insert" parameterType="com.example.minshuku.domain.Room" useGeneratedKeys="true" keyProperty="id">
    <!-- 新規客室を登録する。 -->
    INSERT INTO rooms (
      room_number,
      room_name,
      room_type,
      capacity,
      base_price_per_person,
      private_bath,
      occupancy_status,
      cleaning_status,
      active,
      note
    ) VALUES (
      #{roomNumber},
      #{roomName},
      #{roomType},
      #{capacity},
      #{basePricePerPerson},
      #{privateBath},
      #{occupancyStatus},
      #{cleaningStatus},
      #{active},
      #{note}
    )
  </insert>

  <update id="reactivate" parameterType="com.example.minshuku.domain.Room">
    <!-- 削除済み客室を同一客室番号で再有効化する。 -->
    UPDATE rooms
    SET
      room_name = #{roomName},
      room_type = #{roomType},
      capacity = #{capacity},
      base_price_per_person = #{basePricePerPerson},
      private_bath = #{privateBath},
      occupancy_status = #{occupancyStatus},
      cleaning_status = #{cleaningStatus},
      active = true,
      note = #{note},
      updated_at = now()
    WHERE room_number = #{roomNumber}
  </update>

  <update id="updateStatuses">
    <!-- 宿泊状態と清掃状態は業務上セットで更新する。 -->
    UPDATE rooms
    SET
      occupancy_status = #{occupancyStatus},
      cleaning_status = #{cleaningStatus},
      updated_at = now()
    WHERE id = #{id}
  </update>

  <update id="deactivate">
    <!-- 履歴参照を残すため、削除ではなく論理削除にする。 -->
    UPDATE rooms
    SET
      active = false,
      updated_at = now()
    WHERE id = #{id}
  </update>

  <update id="restore">
    <!-- 論理削除済み客室を一覧から復元する。 -->
    UPDATE rooms
    SET
      active = true,
      updated_at = now()
    WHERE id = #{id}
      AND active = false
  </update>

  <delete id="deletePermanently">
    <!-- 予約履歴が残っていない論理削除済み客室のみ完全削除する。 -->
    DELETE FROM rooms
    WHERE id = #{id}
      AND active = false
  </delete>

  <select id="countBookedReservations" resultType="int">
    <!-- 予約中の客室を論理削除しないため、削除前に有効予約を数える。 -->
    SELECT COUNT(*)
    FROM reservations
    WHERE room_id = #{id}
      AND reservation_status = 'booked'
  </select>

  <select id="countReservationsByRoomId" resultType="int">
    <!-- 予約履歴が残る客室は完全削除できない。 -->
    SELECT COUNT(*)
    FROM reservations
    WHERE room_id = #{id}
  </select>

  <select id="countAll" resultType="int">
    <!-- ダッシュボード集計用の有効客室数。 -->
    SELECT COUNT(*)
    FROM rooms
    WHERE active = true
  </select>

  <select id="countVacant" resultType="int">
    <!-- 空室数は予約可否の参考値として使う。 -->
    SELECT COUNT(*)
    FROM rooms
    WHERE active = true
      AND occupancy_status = 'vacant'
  </select>
</mapper>
