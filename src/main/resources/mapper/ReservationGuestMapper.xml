<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.minshuku.mapper.ReservationGuestMapper">
  <!-- 同行者は予約明細として別テーブルに保存する。 -->
  <insert id="insert" parameterType="com.example.minshuku.domain.ReservationGuest" useGeneratedKeys="true" keyProperty="id">
    <!-- 予約IDに紐づく同行者を 1 件ずつ保存する。 -->
    INSERT INTO reservation_guests (
      reservation_id,
      guest_name,
      guest_kana,
      guest_gender,
      guest_age,
      guest_phone
    ) VALUES (
      #{reservationId},
      #{guestName},
      #{guestKana},
      #{guestGender},
      #{guestAge},
      #{guestPhone}
    )
  </insert>
</mapper>
