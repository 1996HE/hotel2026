# 作業記録 2026-07-07

## プロジェクト

- パス: `/Users/heniantong/Documents/codex/hotel-management`
- システム名: `民宿管理システム`
- 技術構成: Spring Boot 3.3.5 / Java 17 / React / MyBatis / PostgreSQL / JUnit5
- context-path: `/jukai-internal`

## 完了済み作業

- フロントエンドを React 化。
- React bundle: `src/main/resources/static/js/app.js`
- React source: `src/main/frontend/App.jsx`
- React shell: `src/main/resources/templates/app.html`
- CSS: `src/main/resources/static/styles/app.css`
- API Controller: `src/main/java/com/example/minshuku/controller/ApiController.java`

## UI 修正済み

- `/jukai-internal` 配下で API と画面遷移が動くように context-path 対応済み。
- 部屋一覧の状態更新 select が潰れないように列幅とフォーム配置を調整済み。
- 予約一覧、取消予約一覧、チェックアウト一覧に `colgroup` を追加し、表示崩れを修正済み。
- 同行者情報は複数行テキストを `CompanionSummary` でリスト表示するように整理済み。

## 後端 Java 最適化済み

- `ApiController`
  - React API と DTO record を整理。
  - 重複していたページレスポンス生成を `firstPage` に集約。
- `ReservationService`
  - ページ offset 計算を `pageOffset` に集約。
  - 予約登録、重複予約防止、予約番号発番、料金計算、同行者保存、客室状態同期の注釈を追加。
- `RoomService`
  - 客室登録、再有効化、状態更新、論理削除の業務注釈を追加。
- `RoomPriceRuleService`
  - 料金ルール登録、期間重複チェック、削除処理の業務注釈を追加。
- Mapper / Domain
  - DAO/Mapper メソッドに業務用途注釈を追加。
  - Domain フィールドに業務分類コメントを追加。

## 仕様書

参考フォーマット:

- `docs/日本民宿予約管理システム_仕様書_ヘッダー追加.xlsx`

作成済み:

- `docs/仕様書/民宿管理システム.xlsx`
- `docs/仕様書/民宿管理システム_フロントエンド設計書.xlsx`

生成スクリプト:

- `scripts/generate_design_docs.py`
- `scripts/generate_frontend_design_docs.py`

仕様書メタ情報:

- 作成日時: `2026-07-01`
- 更新日時: `2026-07-06`
- 作成者: `何念童`
- 更新者: `何念童`

仕様書フォーマット:

- 参考ファイルと同じ 12 sheet 構成。
- `表紙`
- `変更履歴`
- `概要`
- `業務要件`
- `機能一覧`
- `画面一覧`
- `DB設計`
- `API一覧`
- `非機能要件`
- `テスト仕様`
- `スケジュール`
- `参考資料`

テスト仕様書:

- 単体テスト仕様まで。
- 結合テスト仕様書、総合テスト仕様書は作成しない方針。

## 実行済み検証

- `npm run build`
  - 成功。
- `mvn test`
  - JUnit5 実行。
  - `83` tests。
  - failures `0`。
  - errors `0`。

## よく使うコマンド

```bash
cd /Users/heniantong/Documents/codex/hotel-management
npm run build
mvn test
python3 scripts/generate_design_docs.py
python3 scripts/generate_frontend_design_docs.py
```

ローカル起動:

```bash
mvn spring-boot:run -Dspring-boot.run.arguments=--server.port=8001
```

URL:

```text
http://localhost:8001/jukai-internal/dashboard
http://localhost:8001/jukai-internal/reservations
http://localhost:8001/jukai-internal/rooms
http://localhost:8001/jukai-internal/prices
```

## 明日確認すること

- 最新の仕様書 Excel を開いて、参考ファイルと表頭・列幅・sheet 構成が十分一致しているか目視確認。
- フロントエンド設計書の内容粒度が要件に合っているか確認。
- 必要なら画面キャプチャを仕様書へ追加するか判断。
- `docs/仕様書/README.md` の説明が現在の成果物と一致しているか確認。
- 必要なら `mvn test` と `npm run build` を再実行してから提出用に整理。

## 注意

- `docs/仕様書/~$民宿管理システム.xlsx` は Excel の一時ファイル。編集対象ではない。
- PostgreSQL 接続が sandbox で止まる場合、Spring Boot 起動は権限許可が必要。
- 8001 ポートが使用中の場合は、既存 Java プロセスを確認するか別ポートを指定する。

## 2026-07-07 追加作業

### 詳細仕様書の再整理

- `docs/仕様書/民宿管理システム.xlsx` を再生成。
- `scripts/generate_design_docs.py` を更新。
- 仕様書を 13 sheet 構成へ変更。
  - `表紙`
  - `変更履歴`
  - `概要`
  - `業務要件`
  - `機能一覧`
  - `コード行別処理仕様`
  - `画面一覧`
  - `DB設計`
  - `API一覧`
  - `非機能要件`
  - `テスト仕様`
  - `スケジュール`
  - `参考資料`
- sheet 優先順をプロジェクト標準に合わせて整理。
  - 文書管理
  - 概要
  - 要件定義
  - 基本設計
  - 詳細設計
  - 外部・データ設計
  - 品質設計
  - 管理情報
- `コード行別処理仕様` sheet を `機能一覧` から独立。
- Java コードの処理をファイル単位で整理。
  - `MinshukuManagementApplication.java`
  - `SecurityConfig.java`
  - `DashboardController.java`
  - `ApiController.java`
  - `ReservationService.java`
  - `RoomService.java`
  - `RoomPriceRuleService.java`
  - `ReservationController.java`
  - `RoomController.java`
  - `PriceRuleController.java`
  - `Domain / Mapper Java`
- コード説明の記法を整理。
  - `name = value`
  - `method(args)`
  - `if (condition) -> result`
  - `A -> B -> C`
  - `target = permitAll / denyAll`
- 正規表現は Java コードと同じ形式で記載。
  - `KANA_PATTERN = ^[ァ-ヶー\s]+$`
  - `PHONE_PATTERN = ^\d{3}-\d{4}-\d{4}$`
  - `EMAIL_PATTERN = ^[^\s@]+@[^\s@]+\.[^\s@]+$`
- Excel の可読性を改善。
  - sheet ごとの列幅を個別設定。
  - 行高を文字量に応じて自動調整。
  - `コード行別処理仕様` は `No / コード・要素 / 入出力・状態変化` を読みやすい幅に設定。
  - freeze pane を設定。
  - grid line を非表示。
  - 横向き印刷、幅1ページに調整。

### テスト仕様書の再整理

- `テスト仕様` sheet を拡充。
- テスト要望、テスト目標、前提条件、入力データ・操作、期待結果、判定基準、実施結果を追加。
- テスト用例を `UT-001` から `UT-031` まで整理。
- 対象:
  - `ReservationService`
  - `RoomService`
  - `RoomPriceRuleService`
  - `ApiController`
  - MVC Controller
  - CSRF
  - source-like path 拒否

### フロントエンド設計書の再整理

- `docs/仕様書/民宿管理システム_フロントエンド設計書.xlsx` を再生成。
- `scripts/generate_frontend_design_docs.py` を更新。
- フロントエンド設計書も 13 sheet 構成へ変更。
- `コード行別処理仕様` sheet を追加。
- 前端コードの処理をファイル・機能単位で整理。
  - `App.jsx / 共通初期化`
  - `App.jsx / 共通コンポーネント`
  - `App.jsx / Dashboard`
  - `App.jsx / Rooms`
  - `App.jsx / Prices`
  - `App.jsx / Reservations`
  - `App.jsx / ReservationTable / CheckoutTable`
  - `App.jsx / App mount`
  - `app.html`
  - `app.css / 表示設計`
  - `package.json / build`
- 後端詳細仕様書と同じ記法・表格幅・freeze pane 方針で整理。

### README 更新

- `docs/仕様書/README.md` を更新。
- 詳細仕様書とフロントエンド設計書が 13 sheet 構成であることを記載。
- `コード行別処理仕様`、テスト要望、テスト目標、期待結果を含むことを記載。

### 2026-07-07 追加検証

- 実行済み:

```bash
python3 scripts/generate_design_docs.py
python3 scripts/generate_frontend_design_docs.py
```

- 生成確認済み:
  - `docs/仕様書/民宿管理システム.xlsx`
    - 13 sheets。
    - `コード行別処理仕様` は 252 行。
  - `docs/仕様書/民宿管理システム_フロントエンド設計書.xlsx`
    - 13 sheets。
    - `コード行別処理仕様` は 186 行。

### 明日続きから確認すること

- Excel を実際に開いて、列幅・行高・freeze pane が見やすいか目視確認。
- `コード行別処理仕様` の粒度が提出要件に合うか確認。
- 必要なら前端 `app.css` の後半もさらに細かくコード行別仕様へ追記。
- 必要なら `npm run build` と `mvn test` を再実行して、最終提出前の検証結果を更新。
- Excel 一時ファイル `docs/仕様書/~$民宿管理システム.xlsx` は編集対象外のまま扱う。

## 2026-07-10 継続作業

### 仕様書確認

- `python3 scripts/generate_design_docs.py` を再実行。
- `python3 scripts/generate_frontend_design_docs.py` を再実行。
- `docs/仕様書/民宿管理システム.xlsx` を確認。
  - 13 sheets。
  - `コード行別処理仕様` は 306 行。
  - freeze pane は `コード行別処理仕様` が `A7`、他 sheet が `A6`。
- `docs/仕様書/民宿管理システム_フロントエンド設計書.xlsx` を確認。
  - 13 sheets。
  - `コード行別処理仕様` は 225 行。
  - 変更履歴 `1.6` として app.css 後半の表示仕様追記を反映。

### 追加対応

- `scripts/generate_frontend_design_docs.py` を更新。
- 前端設計書の `app.css / 表示設計` を 17 項目から 43 項目へ拡充。
- 追記対象:
  - 入力欄、ボタン、テーブル固定レイアウト、sticky header。
  - 予約、ダッシュボード、チェックアウト、客室、料金テーブルの min-width。
  - 予約、客室、料金の `colgroup` 列幅。
  - 状態タグ、同行者リスト、代表者表示、行内フォーム。
  - 予約操作、チェックアウト操作、客室操作、ページャー、通知 toast。
  - 同行者入力カード、連絡先なしチェック、レスポンシブ表示。

### 最終検証

- `npm run build`
  - 成功。
  - `src/main/resources/static/js/app.js` を生成。
- `mvn test`
  - 成功。
  - Tests run: 104。
  - Failures: 0。
  - Errors: 0。
  - Skipped: 0。
